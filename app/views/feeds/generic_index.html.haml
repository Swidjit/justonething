%div
  %h1= @title

%div
  = tabbed_item_types('type')

- if @test_output.present?
  Familiar users in descending order
  %ul
    - @test_output.each do |usr|
      %li= usr.display_name

- if @feed_items.count == 0
  %p No items found.
- else
  %ul.feed
    = render :partial => 'items/item', :collection => ItemDecorator.decorate(@feed_items)
