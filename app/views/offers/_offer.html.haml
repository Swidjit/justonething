%ul
  - @offer.messages.each do |message|
    %li= message.text

= form_for :offer_message, :url => offer_offer_messages_path(@offer) do |f|
  = f.hidden_field :offer_id, :value => @offer.id
  = f.text_area :text
  = f.submit 'reply', :as => :button
