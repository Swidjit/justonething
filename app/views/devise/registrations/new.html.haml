%h2= @title || 'Sign up'

%p To take full advantage of Swidjit, you will need to login/register for the site.  unfortunately, those of you that had an account on the demo Swidjit platform will need to create a new one as the information will not be transferred over.

= form_for(@user, :url => registration_path(User)) do |f|

  = error_messages_for(@user)

  %div
    = f.label :display_name
    %br
    = f.text_field :display_name

  %div
    = f.label :first_name
    %br
    = f.text_field :first_name

  %div
    = f.label :last_name
    %br
    = f.text_field :last_name

  %div
    = f.label :zipcode
    %br
    = f.text_field :zipcode

  %div
    = f.label :email
    %br
    = f.email_field :email

  %div
    = f.label :password
    %br
    = f.password_field :password

  %div
    = f.label :password_confirmation
    %br
    = f.password_field :password_confirmation

  %div
    = f.check_box :is_thirteen
    = f.label :is_thirteen, 'I am at least 13 years of age'

  %div= f.submit "Sign up"
